import { HistoricoReserva } from './historico-reserva.model';

describe('HistoricoReserva', () => {
  it('should create an instance', () => {
    expect(new HistoricoReserva()).toBeTruthy();
  });
});
